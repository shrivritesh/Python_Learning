# 📥 Input & Output

This is **Topic 4** of my Python learning journey.

Input and Output allow a Python program to **communicate with the user**.

---

## 📌 What I Learned

- What Input and Output mean
- `input()`
- `print()`
- Taking user input
- Storing input in variables
- `input()` returns a string
- Converting input using `int()`
- Converting input using `float()`
- Using f-strings with input
- Building a practical Personal Introduction project

---

# 🔹 1. What is Input?

**Input** means taking data or information from the user.

Python uses the built-in `input()` function to take input from the user through the keyboard.

### Example

```python
name = input("Enter your name: ")

print(name)